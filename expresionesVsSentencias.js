/** Sintaxis de lenguaje de programación:
 * las reglas que definen como podemos combinar distintos simbolos para
 * producir instrucciones validas y asi crear un programa que la computadora
 * entienda.
 * Expresiones: unidades de codigo que producen una valor al ser evaluadas, algunas
 * pueden tener efectos secundarios.
 * los operadores son un ejemplo claro de expresiones.
 * ej: 10 + 10 = 20.
 * Sentencias: las acciones que son ejecutadas para que nuestro programa siga
 * la logica que queremos.
 */

//expresiones primarias = cualquier valor primitivo, al ser evaluadas producen
//el mismo valor.

// expresion de funcion = Cuando escribimos una funcion en un lugar del codigo
// donde se espera un valor.

// podemos usar una propiedad de un objeto como una expresion
unaVariable = miObjeto.nombre;
// la invocacion de una funcion tambien es una expresion.

// el ; marca el final de una instruccion en javascript

// Sentencia (statement) o instrucciones : es una accion que javascrpt ejecuta
//  para que ocurra algo,
//para que avance la logica de nuestro programa

// Declaracion de funcion (function declaration) Sirve para crear una funcion
// con el nombre y los parametros indicados dentro del scope donde se encuentra
// esa sentencia.

// el hoisting es la accion que realiza javascript al revisar el programa
// puesto que eleva las funciones.

// Sentencias de control: permiten alterar el flujo normal de ejecucion. Podemos
//decidir que flujo va a seguir la ejecucion de nuestro programa.

// sentencia throw sirve para mandar una escepcion cuando hay un error en la logica
// de nuestro programa.